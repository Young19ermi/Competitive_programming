n,m = input().split()
nums = list(map(int, input().split()))
nums.sort(reverse = True)
minimum = 1000000000
for i in range(4):

    for j in range(i+1,len(nums)):
        if nums[i] != nums[j]:
            minimum = min(minimum,nums[i]- nums[j])
print(minimum)




num.sort()


for i in range(lne(nums)):
    if nums[i] != nums[j]:
        minimum = min(minimum, nums[i] + nums[j])
    for j in range(len(nums)):
        for j in range(len(nums)):
            minimum = min(nums[i], minimum )
def solve(nums):
    if len(set(nums)) == 1:
        return nums[0]

    for letter in words:
        index = ord(letter) - ord('a')
        id node.children[index] is None:
        node.children[index] = TrieNode()
def insert(word):
    node= self.root
    for letter in word:
        index = ord(letter) - ord('a'
        for jj in range(len(nums))):
            nums[i] += nums[j]
        