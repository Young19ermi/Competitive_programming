#linear search
array=[1,3,4,5,6,7,5]
def search(array,n):
	for index in range(len(array)):
		if array[index]==n:
			print('found at: ' + str(index+1))
			break
	else:
		print("not found")
n=int(input("enter your number: "))
search(array,n)
