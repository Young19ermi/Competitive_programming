class Solution:
    def reverse(self, x: int) -> int:
        if x>=0:
            r=int(str(x)[::-1])
        else:
            r=-int(str(-x)[::-1])
        return r if -2*31<=abs(r)<=2**31-1 else 0        
        
        
